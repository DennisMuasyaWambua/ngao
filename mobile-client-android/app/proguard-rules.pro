# Add project-specific ProGuard/R8 rules here.
# Retrofit, OkHttp and Gson ship their own consumer rules, so the scaffold needs
# no custom keep rules yet. Add model-class keep rules here if you enable R8
# minification for release builds.
